{

  "variables": {
    "source_image": "{{ env `IMG_ID` }}",
    "image_name": "{{ env `IMG_NAME` }}",
    "factory_network": "{{ env `NET_ID` }}",
    "factory_security_group_name": "{{env `SG_ID`}}"

  },


  "builders": [{
    "type": "openstack",
    "image_name": "{{ user `image_name` }}",
    "source_image": "{{ user `source_image` }}",
    "flavor": "16",
    "networks": "{{ user `factory_network` }}",
    "floating_ip_pool": "public",
    "security_groups": ["{{ user `factory_security_group_name` }}"],
    "ssh_username": "cloud",
    "ssh_pty": true,
    "ssh_timeout": "5m"

  }
  ],
  "provisioners": [


    {
      "type": "ansible",
      "playbook_file": "{{ user `image_name` }}/bootstrap.yml",
      "host_alias":"local"

    }


      ]

}